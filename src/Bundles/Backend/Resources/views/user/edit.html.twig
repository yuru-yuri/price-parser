{% extends 'backend.html.twig' %}

{% block title %}Edit user{% endblock %}

{% block body %}

    <div>

        <h2>{{ user.name }}</h2>

        <div>
            <a href="{{ path('users_backend') }}">Users</a>
        </div>

        <div>
            {#{{ form(form) }}#}
            {{ form_start(form) }}
                <button type="submit">Send</button>
            {{ form_end(form) }}
        </div>

    </div>

{% endblock %}
